[project]
name = "auth"
version = "0.1.0"
description = "Authentication service for CloudGIT WEB Application"
license = "Apache-2.0"
authors = [
    {name = "Sam Nazarov", email = "samnnazarov@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10,<3.11"
dependencies = [
    "django (>=5.2.1,<6.0.0)",
    "django-cors-headers (>=4.7.0,<5.0.0)",
    "python-dotenv (>=1.1.0,<2.0.0)",
    "djangorestframework (>=3.16.0,<4.0.0)",
    "gunicorn[gevent] (>=23.0.0,<24.0.0)",
    "pika (>=1.3.2,<2.0.0)",
]

[project.optional-dependencies]
postgres = [
    "psycopg2-binary"  # poetry install --extras postgres
]
common = [
    "common @ ../../common"  # poetry install --extras common
]

[tool.poetry]
packages = [
    {include = "common", from = "../../common/src"},  # poetry install --only-root (for dev install in editable mode)
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
