import React from "react";

const DropdownBar = ({children}) => {
    return (
        <div className="dropdown  dropdown-dummy">
            {children}
        </div>
    );
}

export default DropdownBar;
